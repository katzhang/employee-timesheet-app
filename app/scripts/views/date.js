/*global app, Backbone, JST*/

app.Views = app.Views || {};

(function () {
    'use strict';

    app.Views.DateView = Backbone.View.extend({

        initialize: function () {
            this.render();

            

        }
    });

})();
